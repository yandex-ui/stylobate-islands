// Proto for simple buttons
skin-proto-button()
  position: relative

  color: #333

  border-radius: 3px
  box-shadow: 0 1px 0 rgba(#000, 0.07)

  &:before
    content: ""
    kind: fill 1px
    border-radius: 2px

  if small in arguments
    skin-p2(no-margin)
    padding: (gap_xs - 0.1*rem) (gap_m - 0.3*rem)
  else
    skin-p(no-margin)
    padding: (gap_xs - 0.1*rem) (gap_m - 0.1*rem)

  & > .button-content
    kind: block

  &:not(.is-disabled):active
    skin-button_pressed()

    & > .button-content
      top: 1px

  &:not(.is-disabled):focus
    outline: 0
    box-shadow: 0 1px 0 rgba(#000, 0.07), 0 0 6px 2px rgba(255,204,0, 0.7)

    &:before
      box-shadow: 0 0 0 1px rgba(193,154,0, 0.2);

// Normal button
skin-button()
  skin-proto-button(arguments)

  background: #EEE
  background: -webkit-linear-gradient(rgba(#000, 0.2), rgba(#000, 0.4))

  &:not(.is-disabled):hover
    skin-button_hover()

  &:before
    background: #FFF
    background: -webkit-linear-gradient(#FFF, #EEE)


// Action button
skin-action-button()
  skin-proto-button(arguments)

  background: #FFDF60
  background: -webkit-linear-gradient(rgba(96,64,0, 0.4),rgba(70,47,0, 0.54))

  &:not(.is-disabled):hover
    skin-action-button_hover()

  &:before
    background: #FFDF60
    background: -webkit-linear-gradient(#FFDF60, #FFCC00)


// Hover state
skin-button_hover()
  background: -webkit-linear-gradient(rgba(#000, 0.22), rgba(#000, 0.4))

  &:before
    background: -webkit-linear-gradient(#FFF, #F6F5F3)

// Hover state
skin-action-button_hover()
  background: -webkit-linear-gradient(rgba(#000, 0.22), rgba(#000, 0.4))

  &:before
    background: -webkit-linear-gradient(#FFE681, #FFD428)

// Pressed state
skin-button_pressed()
  box-shadow: inset 0 2px 1px -1px rgba(#000, 0.4), 0 1px 0 rgba(#FFF, 0.2)

  &:before
    box-shadow: inset 0 1px 3px -1px rgba(#000, 0.5)

