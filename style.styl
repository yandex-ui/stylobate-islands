// Using stylobuild
use('node_modules/stylobuild', {
  ie: ie
})

@require "stylobate"

// Skins
$init_islands_root = false
@require "skins/islands"
set-skin-namespace('islands')

// Shortcuts to variables
$xs = $islands_xs
$s = $islands_s
$m = $islands_m
$l = $islands_l

$text_m = $islands_text_m
$text_s = $islands_text_s

$color_text = $islands_color_text
$color_text_misc = $islands_color_text_misc

// Global reset for things
reset('h1' 'h2' 'h3', global)

// Page
@require "structures/page/page"

// Blocks
@require "blocks/is-hidden"
@require "blocks/is-disabled"
@require "blocks/site-nav/site-nav" // Rename to tabs/nav/smth like that?
@require "blocks/text/text"
@require "blocks/example/example"
@require "blocks/link/link"
@require "blocks/form/form"

// Structures
@require "structures/header/header"
@require "structures/header/header-logo"
@require "structures/header/header-service"

@require "structures/content/content"

@require "structures/aside/aside" // Move vertical nav out of here?
