skin-islands-v3-button_shape($this)
  $shape = $this.parent.modifiers['shape'] || 'normal'

  if $shape in ('normal' 'rect')
    border-radius: 3px

  if $shape in ('round' 'circle')
    border-radius: 999px
