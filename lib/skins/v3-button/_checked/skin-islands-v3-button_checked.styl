// TODO: check on different types (like dark one with colors)

skin-islands-v3-button_checked($this)
  border-color: rgba(#997A00, 0.5)

  $background = $this.colors[0] || #FFEBA0

  if 'content' in $this.elements
    +optional-element($this, 'content')
      background: $background
  else
    box-shadow: inset 0 90px $background
