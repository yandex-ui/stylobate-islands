skin-islands-select($this)
  z-index: 10

  -webkit-appearance: none
  -moz-appearance: menuitem
  -moz-appearance: window
  cursor: pointer

  width: 100%
  border: none
  padding: 0
  margin: 0

  font: inherit

  color: inherit
  background: transparent

  &:focus
    outline: none

  // Removing dotted outline from Fx
  &, /x::-moz-focus-inner
    color: transparent
    text-shadow: 0 0 0 ($this.colors[0] or $islands_color_text)

  // Hardcoded paddings, yeah, Opera and Fx are stupid there
  unless no-paddings in $this.arguments
    skin-islands-select__padding($this)

skin-islands-select__padding($this)
  if small in $this.arguments
    $sides = ($islands_xs - 1px) (2*$islands_xs + $islands_m - 3px) ($islands_xs - 1px) ($islands_m - 3px)
  else
    $sides = ($islands_xs - 1px) (2*$islands_xs + $islands_m - 1px) ($islands_xs - 1px) ($islands_m - 1px)

  padding: $sides[0] $sides[1] $sides[2] $sides[3]

  // Fixing padding in Fx
  &, /x::-moz-focus-inner
    // Here in for opera one-liners 'cause there is a bug in Stylus
    padding: ($sides[0] + 1px) ($sides[1] - 4px) ($sides[2] + 1px) ($sides[3] - 4px) unless small in $this.arguments
    padding: $sides[0] ($sides[1] - 4px) $sides[2] ($sides[3] - 4px) if small in $this.arguments

  // Fixing margins in Opera
  &, /x:-o-prefocus
    box-sizing: content-box

    margin-right: -15px
    padding-top: ($sides[0] + 2px)
    padding-bottom: ($sides[0] + 2px) unless small in $this.arguments
    padding-bottom: ($sides[0] + 1px) if small in $this.arguments

skin-islands-select__focus()
  select:focus + &
    kind: fill
    z-index: 9
    skin-islands-button_focus_style()


skin-islands-select__wrap()
  padding: 0

  // In Opera the va suxx, also hiding arrow using oh
  &, /x:-o-prefocus
    overflow: hidden
    vertical-align: middle

  &:after
    position: absolute
    top: 50%
    right: 2*$islands_xs
    bottom: auto
    left: auto
    margin-right: -2px

    global_skin: islands-icon_dropdown #000
