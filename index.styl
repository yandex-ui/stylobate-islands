unless check-stylobate-version is defined
  check-stylobate-version($version)
    error('Used Stylobate version is very outdated, update it to the `' + $version + '`.')

check-stylobate-version('0.18.x')

@import "lib/index.styl"
